package com.cmput301w23t00.qrquest.ui.updateavatar;

// this was an interface previously in AddCityFootPrintFragment this was how we were told to do it in lab 3 but it put it in it's own file, because it was easier to make the UML
public interface EditAvatarDialogListener {
    void updateProfilePicture(int id);
}